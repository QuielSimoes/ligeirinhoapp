import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.ligeirinho',
  appName: 'Ligeirinho',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
